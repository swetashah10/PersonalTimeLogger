package main.java;

public class WriteToSheets {

	/*
	 * void WriteExample() throws IOException { Sheets service =
	 * getSheetsService(); List<Request> requests = new ArrayList<>();
	 * 
	 * List<CellData> values = new ArrayList<>();
	 * 
	 * values.add(new CellData().setUserEnteredValue(new
	 * ExtendedValue().setStringValue("Hello World!"))); requests.add(new
	 * Request().setUpdateCells( new UpdateCellsRequest().setStart(new
	 * GridCoordinate().setSheetId(0).setRowIndex(0).setColumnIndex(0))
	 * .setRows(Arrays.asList(new RowData().setValues(values)))
	 * .setFields("userEnteredValue,userEnteredFormat.backgroundColor")));
	 * 
	 * BatchUpdateSpreadsheetRequest batchUpdateRequest = new
	 * BatchUpdateSpreadsheetRequest().setRequests(requests);
	 * service.spreadsheets().batchUpdate(spreadsheetId,
	 * batchUpdateRequest).execute(); }
	 */
}
